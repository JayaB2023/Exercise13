# TUPLES

# This prints 5 as the length of the tuple
tup = 'Hello'
print(len(tup))

# When adding a comma, it prints 1 to show one word or one string
# when we add 'hi', it prints 2
tup = 'Hello', 'hi',
print(len(tup))

